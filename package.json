{
  "name": "bingo-board",
  "version": "1.0.0",
  "type": "module",
  "files": [
    "build"
  ],
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "dev": "tsc --watch --preserveWatchOutput",
    "build": "npm run clean && tsc -p tsconfig.json && cp index.html CNAME build/",
    "clean": "rm -rf build",
    "serve": "python3 -m http.server 4173"
  },
  "dependencies": {
    "lit": "^2.6.1",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.191",
    "typescript": "^4.9.3"
  }
}
